export const AUTH_CONSTANTS = {
  SESSION_PREFIX: 'session:',
  IDEMPOTENCY_PREFIX: 'idempotency:',
  BCRYPT_ROUNDS: 12,
  TOKEN_TYPE: 'Bearer',
  GLOBAL_ADMIN_EMAIL: 'global_admin@example.com'
} as const;
